// TEMPLATE — copy to src/features/<feature-name>/<Feature>.tsx and replace the
// __PLACEHOLDERS__. A component only renders; it pulls state from its hook and
// holds no business logic (see .agents/project/ARCHITECTURE.md).

import { use__Feature__ } from './use__Feature__';

export function __Feature__() {
  const { /* values + handlers from the hook */ } = use__Feature__();

  return (
    <section className="__feature-name__" aria-labelledby="__feature-name__-heading">
      <h2 id="__feature-name__-heading" className="__feature-name____heading">
        __Title__
      </h2>

      {/* Render observable state. Keep every interactive element a real,
          labelled control so the behaviour map and a11y tooling can reach it. */}
    </section>
  );
}
