# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test + coverage artefacts
coverage/
playwright-report/
test-results/
.swc/

# StyleProof capture output (regenerated each run)
__stylemaps__/

# Lighthouse CI output
.lighthouseci/

# graphify knowledge graph (generated — rebuild with the graphify skill or
# ArchitectPlaybook's /pre-audit-setup). Commit only the human-readable report
# as a showcase artifact; the graph data, HTML, and cache are regenerable.
graphify-out/*
!graphify-out/GRAPH_REPORT.md

# GitNexus local repo index (generated — rebuild with `gitnexus analyze`)
.gitnexus/

# Local env + editor noise
.env
.env.*
!.env.example
.DS_Store
*.log

# Claude Code personal overrides (never committed; team settings live in settings.json)
.claude/settings.local.json
