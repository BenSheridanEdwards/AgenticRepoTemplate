# Peer deps (React 19 + the testing-library ecosystem) install automatically and
# do not hard-fail the install, so onboarding a new machine is one command.
auto-install-peers=true
strict-peer-dependencies=false
